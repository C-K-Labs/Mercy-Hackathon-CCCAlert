{
  "name": "cccalert-merged2",
  "version": "1.0.0",
  "description": "Unified campus safety server: Gemini AI + incident classification dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:client": "cd client && npm install && npm run build",
    "setup": "npm install && npm run build:client"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "better-sqlite3": "^12.9.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "fluent-ffmpeg": "^2.1.3",
    "resend": "^4.0.0",
    "twilio": "^5.3.3"
  }
}
